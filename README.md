# VDG_FreightForwarder_Publish_LBS

LBS public static release of VDG FreightForwarder (GitHub Pages, served from `main`/`docs`).
Built locally from the private repo via `tools/publish.sh LBS <tag>` — no GitHub CI build.
Gated by Google OAuth + an LBS-issued licence. Kill switch: disable Pages / make private.
